# 项目优化路线图

> 更新时间：2026-08-29
>
> 本文记录符玄桌宠当前已经验证的优化、下一阶段实施顺序和验收标准。文中“已完成”只表示代码已构建并通过现有验证；未验证的收益不写成既定事实。

## 一、当前基线

项目是 Tuanjie 2022.3.62t7 + Live2D + Unity C# + Node.js bridge + Python office generator 的 Windows 桌面助手。

当前主要风险不再是“约 5 元/天”的 Key 归属问题。该费用已确认来自其他项目复用同一个用户级 `DEEPSEEK_API_KEY`，后续仍应使用按项目隔离的 Key 和账单记录。

最近三个提交形成了第一轮稳定性基线：

| 提交 | 内容 | 状态 |
|------|------|------|
| `60e264b` | 统一退出生命周期、外置窗口竞态保护、ChatManager/Live2D partial 拆分、桥接错误 JSON 契约 | 已验证 |
| `6a524b0` | 桥接任务非阻塞取消、取消响应 `success` 校验、网络错误不可重试分类 | 已验证 |
| `e774c74` | ToolRegistry 惰性初始化、异步协程异常边界 | 已验证 |
| `b1261ca` | C# 桥接请求统一经 `ConfigureRequest()` 配置鉴权、JSON 请求头和 timeout | 已验证 |
| `3d92475` | `ChatManager.ContextBuilder.cs` 拆出 SystemPrompt 上下文注入，保持顺序和预算规则不变 | 已验证 |
| `487dbe8` | `ChatManager.ToolLoop.cs` 承载 `DoToolLoop()` 主体、危险工具确认、工具执行、结果压缩与历史写回，保持回环行为不变 | 已验证 |
| `9a1d7dd` | `ChatManager.ReplyFinalizer.cs` 抽出最终回复发布与逐句队列触发，保持云端、本地和工具循环上限路径顺序不变 | 已验证 |
| `e8bcc7d` | `ChatManager.ReplyFinalizer.cs` 接管明确记忆写入和周期性摘要，保持测试隔离、记忆治理和异步回退行为不变 | 已验证 |
| `b309a0b` | 修复请求失败状态被覆盖为「就绪」及队列新协程引用被旧协程清空的问题 | 已验证 |

验证基线：

- `build.ps1 -Quick` 通过。
- `build.ps1` 完整播放器构建通过。
- `node --check` 通过。
- `runtime_smoke.cjs --verbose` 通过：UI、外置窗口、审批、表情、三档尺寸、完整退出、零 NRE、生产记忆零污染。
- `logs/build/test_results.xml` 仍是历史的 114/114 记录，不能当作本轮新鲜测试统计。

## 二、执行看板

状态定义：✅ 已完成并验证；🔧 正在实施；⏳ 待实施；⚠️ 仅部分验证；🚫 刻意不做。

| 编号 | 工作包 | 状态 | 依赖 | 下一步产物 |
|------|--------|------|------|------------|
| O-01 | 退出生命周期与外置窗口启动竞态 | ✅ | — | 保持回归 |
| O-02 | 桥接错误契约、取消和请求配置 | ✅ | — | 保持回归 |
| O-03 | ToolRegistry 初始化与异步异常边界 | ✅ | — | 增加 mock/contract 测试 |
| O-04 | ChatManager 请求生命周期分层 | ✅ | O-01/O-02 | 保持行为对照 |
| O-05 | ChatManager 上下文构建分层 | ✅ | O-04 | 继续保持缓存前缀稳定 |
| O-06 | ToolLoopCoordinator：工具回环、审批、结果压缩 | ✅ | O-03/O-04 | 保持工具回环回归 |
| O-07 | ReplyFinalizer：回复收尾、记忆和质量遥测 | 🔧 | O-06 | 核心收尾已完成；继续补充质量裁判的新鲜测试与案例 |
| O-08 | Live2D 普通参数数据化 | ⏳ | 可见播放器回归能力 | 模板迁移清单 + 动作截图 |
| O-12 | Live2D 角色局部 RT 与高刷新率 | ⚠️ | — | 可见播放器 GPU/帧率与裁切对照；保留全屏窗口交互 |
| O-13 | AI 运行时输入模拟与拖动挣扎修复 | ✅ | — | `@@sim:*` 隔离命令覆盖；目标点/速度提前写入 Cubism physics 输入；Unity 截图闭环已验证左右拖动及镜像后方向，响应增益已降幅 |
| O-14 | Live2D 参数写入缓存与物理刷新观测 | ✅ | O-08/O-12 | 模型加载后缓存 `CubismParameter`；所有 `ForceUpdateNow()` 统一统计入口；不改变视觉/物理条件，实际 CPU/GPU 收益待 Profiling |
| O-15 | IMGUI 星空动画更新边界 | ✅ | — | `StarField` 状态更新移至 `RightPanel.Update()`，`OnGUI` 仅绘制；Quick/完整构建/隔离冒烟通过，实际性能收益待 Profiling |
| O-16 | 局部 RT 取景重复计算收敛 | ✅ | O-12 | `UpdateOverlayFraming()` 普通帧只在物理完成后的 `LateUpdate()` 执行一次；拖拽/点击提前返回路径补齐同步；Quick/完整构建/隔离冒烟通过，实际 CPU/GPU 收益待 Profiling |
| O-09 | 外置窗口真实可见多轮回归 | ⏳ | O-01 | 启动/隐藏/恢复/关闭记录 |
| O-10 | EditMode 结果文件新鲜度治理 | ⏳ | 构建脚本 | 新鲜 XML 或独立结果摘要 |
| O-11 | bridge/Gateway/Ollama 启动依赖治理 | ⏳ | 安装器现状确认 | 四种安装/启动场景验证 |

### 执行顺序

本计划按以下顺序推进，不跨越未通过的阶段门：

1. **阶段 A（已完成）**：生命周期、桥接契约、ToolRegistry 基础边界。
2. **阶段 B（已完成）**：ChatManager 分层；已完成 `ContextBuilder` 与 `ToolLoopCoordinator` 首轮拆分。
3. **阶段 C（首轮已完成）**：已拆 `ReplyFinalizer` 并核对记忆写入、质量遥测和请求状态收尾；质量裁判案例与新鲜测试证据仍在补齐。
4. **阶段 D-1（代码已完成，实测待补）**：Live2D 角色局部 RT 与 60/45/30 目标帧率；需要可见播放器 GPU、实际 FPS 和裁切对照。
5. **阶段 D-2**：Live2D 普通参数模板化，只迁移可回归参数，保留 P0 安全网。
6. **阶段 D-3（已完成）**：增加 `@@sim`/`@@input` 运行时模拟与 Unity 截图闭环，修复拖动输入丢失、边界越界、失焦残留和挣扎迟滞；接入目标点式拖拽物理输入并降低过强响应；Quick/完整构建/隔离冒烟及左右拖动截图回归通过。
7. **阶段 E**：外置窗口可见回归、EditMode 结果治理和安装器依赖治理。

每个阶段必须先完成代码验证和文档更新，才进入下一个阶段；如果测试只证明编译成功，状态最多标为 ⚠️，不能标为 ✅。

## 二、优化原则

1. 先消除状态和资源生命周期风险，再做性能和结构重构。
2. 云端调用、Token、记忆写入和危险工具必须有明确边界，不能用“看起来没问题”替代测量。
3. 每次只改一个责任边界；partial 拆分必须保持公开 API 和执行顺序不变。
4. 先用隔离数据目录测试，再运行完整播放器；测试失败时不更新“已验证”文档结论。
5. P0 安全网、危险工具审批、Live2D Physics 顺序和外置窗口销毁顺序不为追求整洁而迁移。

## 三、下一阶段计划

### P0：稳定性和可恢复性

| 方向 | 具体工作 | 验收标准 |
|------|----------|----------|
| 桥接请求边界 | ✅ 已完成第一步：统一 `ConfigureRequest()` 的鉴权、JSON 请求头和 timeout；后续仍可继续整理响应读取和 correlation id | 不改变端点响应；HTTP 错误、超时、空响应均有稳定错误文本；Node 语法和 C# 构建通过 |
| 任务状态机 | 明确 queued/running/done/error/cancelled 的状态转移，补充重复取消、退出中取消、取消后迟到响应处理 | 退出时不再继续轮询；取消接口返回 false 时上层不会误报成功 |
| 外置窗口 | 增加真实可见播放器下的启动、隐藏、恢复、关闭多轮回归 | 不出现窗口线程残留、透明窗口消失或退出崩溃；不能只用强制杀进程作为通过依据 |
| 测试产物 | 修复或替代 EditMode XML 不刷新的问题，保留明确的测试时间、用例数和退出码 | 每次测试能区分“编译成功”“测试入口成功”“测试结果文件新鲜” |

### P1：结构和可维护性

#### 1. ChatManager 继续分层

当前已把请求生命周期抽到 `ChatManager.RequestLifecycle.cs`，完成 `ChatManager.ContextBuilder.cs` 上下文拆分，并完成 `O-06 ToolLoopCoordinator` 的首轮迁移：

- `ChatManager.ContextBuilder.cs`：角色卡、记忆、偏好、任务轨迹、时间天气等上下文注入（已完成第一步，暂保持 partial 以减少行为风险）。
- `ChatManager.ToolLoop.cs`：承载 `DoToolLoop()` 主体、危险工具审批等待、工具执行、`openclaw_task` 熔断、结果压缩和 tool history 写回。
- `ChatManager.ReplyFinalizer.cs`：已抽出最终回复发布、逐句队列触发、明确记忆写入和周期性摘要；请求失败状态与队列协程引用已加固，质量遥测仍待后续拆分。

约束：缓存前缀顺序不能改变；`PromptContextBudget`、`ToolResultBudget` 和 `QualityTelemetry` 必须继续生效；拆分后先做行为对照，再做性能优化。

### 阶段门：ReplyFinalizer 后续拆分条件

- 先记录当前回复收尾的入口、流式缓冲刷新、`_lastReply` 更新、记忆写入、质量遥测和请求状态收尾顺序。
- 第一刀已完成：只移动回复发布方法，没有改变本地/云端分支、流式显示、`RecordConversationMemory` 和 `QualityTelemetry` 逻辑。
- 下一刀只处理质量遥测收尾，必须继续保持代际守卫、队列调度和反思后台任务顺序。
- 必须通过 `build.ps1 -Quick`；由于涉及请求状态和记忆写入，还必须通过完整构建、隔离冒烟和无云端污染检查。
- 测试通过后更新 `docs/modules/ai-chat-system.md` 和本文看板。

#### 2. ToolEngine 调度治理

- 为 `ToolRegistry` 增加安全的重复注册报告和注册快照，避免反射扫描后工具数量悄悄变化。
- 为同步工具、异步工具、危险工具建立不执行真实副作用的 mock/contract 测试。
- 统一工具失败结果的格式和日志级别，避免异常文本直接进入云端上下文。
- 保持危险工具清单与审批链单一来源，禁止在 `ChatManager`、本地路由和工具实现中各自维护一份不一致的清单。

#### 3. Live2D 参数治理

- 已完成第一步：模型加载后缓存 `CubismParameter` 引用，`SetParameter()`、调试偏移和参数范围查询不再反复执行 `FindById()`；所有 `ForceUpdateNow()` 统一经过统计入口。该改动保留原有物理顺序和调用条件，实际 CPU/GPU 收益仍待 Profiling。
- 继续把普通动作参数迁移到 `Live2DMotionTemplates` 或语义映射配置。
- `P0` 眼睛保护、Physics 顺序、姿态锁和停止过渡保护继续保留在代码安全网中。
- 每次迁移必须做动作截图或可见播放器回归，不能只以编译通过作为完成标准。

#### 4. IMGUI 绘制节奏

- 星空运动状态已从 `OnGUI` 绘制流程移至 `RightPanel.Update()`，避免 IMGUI 的多次 `Layout`/`Repaint` 事件放大动画更新次数；本轮构建与隔离冒烟已通过，实际 CPU/GPU 收益待 Profiling。

### P2：性能、成本和观测

#### 1. Token 和云端成本

- 先按 `source/task/case_id` 收集真实 usage，再决定是否压缩上下文或改变模型路由。
- 继续保持本地模型优先、测试模式禁云端、云端失败不盲目重试。
- 对 OpenClaw 任务记录步骤数、耗时、取消原因和最终状态，区分“任务失败”和“桥接失败”。

#### 2. 运行时观测

- 为启动、桥接健康、任务提交、轮询、取消、审批和退出增加统一 correlation id。
- 日志中记录阶段、耗时和结果类别，不记录 Token、API Key 或完整敏感参数。
- 逐步把“日志搜索”变成可统计指标，减少依赖人工翻 Player.log。

#### 3. 启动依赖

- 独立整理 bridge/Gateway/Ollama 的启动、健康检查和失败恢复。
- 安装器改动单独提交，先验证全新机器、已有服务、无网络和权限不足四种路径。

### P3：暂不优先

- 3D 渲染分支：当前仍是 TODO，不纳入本轮优化。
- 全面替换 IMGUI：先解决状态和回归能力，再评估 UI 框架迁移。
- 大规模重写 `ExternalChatWindow` 或 `DesktopPet`：这两个类与 Win32、Unity 生命周期耦合较深，必须在测试覆盖增加后再拆。
- 动态分辨率全局降级：当前 Live2D RT 缩放已有性能档位入口，但主窗口分辨率策略仍保持现状，不能把“有入口”写成“已完成动态降级”。

## 四、每项优化的实施闭环

1. 先读 `AGENTS.md`、`docs/README.md`、构建规范、代码真相架构和对应模块文档。
2. 写出当前行为和目标行为，标明是否涉及云端、危险工具、外置窗口或数据写入。
3. 只做一个边界内的最小修改，并补确定性测试或运行时命令。
4. 执行 `build.ps1 -Quick`；涉及播放器、渲染、窗口或退出时追加完整构建和隔离冒烟。
5. 测试通过后再更新对应模块文档和本文状态。
6. 使用 Conventional Commit 单独提交，不把临时脚本、安装器草稿和未确认改动混入代码提交。

## 五、完成定义

一项优化只有同时满足以下条件，才可以标记为完成：

- 代码边界清楚，未引入重复安全策略或新的全局状态。
- 快速构建通过；涉及运行时的改动有完整构建和隔离冒烟记录。
- 测试使用临时数据目录，生产记忆文件 mtime 未变化。
- 失败路径、取消路径和退出路径至少有一个确定性验证。
- 文档描述的是实际验证过的代码，而不是预期收益。
- 提交信息能单独说明改动目的，后续可以回溯和回滚。
