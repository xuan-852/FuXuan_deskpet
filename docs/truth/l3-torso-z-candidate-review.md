# L3 躯干 Z 轴候选：双模型自然度评审

> **证据日期**：2026-09-17  
> **状态**：裁剪复核已解决全帧评审尺度分歧；保留为 `Conditional` 候选，未认证、未注册、未开放运行时。

## 评审输入

- 隔离根：`%TEMP%\\fuxuan_torso_dynamic_z_20260917`，含 `.test_mode`。
- 证据包：`candidate-review-packet-torso_physics_lean_z.json`，25 帧，SHA-256 `03f4ac34a6c8638f08f97c4bb0eb9df2188bb75387745cf175fd400ba098c1da`。
- 语义边界：保守幅度的全身躯干侧倾并回到基线；不是行走、位移、手臂动作或可直接开放的运行时技能。
- 用户已明确同意仅把这些模型截图外发给 DeepSeek 和 GLM 各一次。无桌面、聊天或用户数据外发。

## 双模型结果

| 提供方 | 结论 | 连续性 | 复位 | 步行基线 | 自然度 |
|---|---|---|---|---|---:|
| DeepSeek | `semantic_boundary=unsupported`；认为抽取帧几乎一致、未见可辨识侧倾 | 否 | 是 | 否 | 10 |
| GLM `glm-4.5v` | `semantic_boundary=supported`；认为可见平滑右侧倾、回正且无明显故障 | 是 | 是 | 是 | 92 |

DeepSeek 使用 2,667 tokens；GLM 使用 5,100 tokens。两份提供方响应和用量均仅保存在隔离根的 `cloud_review/`，不入版本库。

## 裁决边界

两方对“是否可见连续躯干侧倾”“是否达到步行基线”及自然度评分均发生实质冲突。根据 L3 指南的自然度硬门槛与产品方向的人工升级规则，本候选不能被主代理自动裁决：

- 不得升为 `Supporting` 或 `Certified`；
- 不得注册为技能、修改映射、接入 LLM 或运行时；
- 仍保留此前的 `Conditional` 机械候选状态；
- 等待人工查看隔离帧与两份评审响应后，决定采用更大但仍安全的幅度、改用局部裁剪评审，或终止该候选。

## 局部裁剪补充证据（未外发）

为排查全帧尺度造成的判读下限，已在同一隔离根内从 768×768 原始模型帧确定性裁出 `x=170,y=130,width=430,height=500` 的躯干区域。25 张裁剪帧和 `candidate-review-packet-torso_physics_lean_z_cropped.json` 仍在 `.test_mode` 根内；裁剪包 SHA-256 为 `50e41093614d7ac3bf43716840fbff5f7f242994e1b994a9b18f655747f25e2f`。

裁剪基线到峰值的平均 RGB 像素差为 `20.213577`，高于全帧采样指标。人工查看裁剪后的基线、峰值与复位帧可见躯干/服饰重心变化及回归；这支持“全帧分辨率可能影响可见性判断”的诊断，但不是新的自然度结论，未向任何提供方外发，也不改变 `BlockedByDecision` 状态。

## 裁剪双模型复核（已授权外发）

用户随后明确同意将同一隔离根的裁剪帧分别发送给 DeepSeek 和 GLM 各一次。两方均以裁剪证据包 `50e41093614d7ac3bf43716840fbff5f7f242994e1b994a9b18f655747f25e2f` 返回高置信一致结论：

| 提供方 | 语义 | 连续性 | 复位 | 步行基线 | 自然度 | 用量 |
|---|---|---|---|---|---:|---:|
| DeepSeek | supported | 是 | 是 | 是 | 85 | 1,469 tokens |
| GLM `glm-4.5v` | supported | 是 | 是 | 是 | 92 | 1,942 tokens |

两份响应均确认保守幅度的躯干小幅侧倾、平滑回正、无明显穿模或突跳。由此解决了原全帧评审的尺度分歧：该分歧不再是 `BlockedByDecision`，且不得以全帧 DeepSeek 的 10 分覆盖更高分辨率的同源证据包结论。

此共识只完成本候选的视觉/语义/自然度评审证据；它不取代正式 `SkillCertificationRecord` 所需的版本、资源、前后姿势、运行时安全恢复与注册审查。候选继续为 `Conditional`，不自动升为 `Certified` 或对 LLM 开放。

## 已验证的本地事实

本地物理探针中 `ParamBodyAngleZ` 的 `[-0.75,0.75]` 范围三次、12 步采样均 `resetStable=true`、复位差为 0；这只证明离散稳定和可复位，不足以解决双模型对视觉自然度的分歧。详见 [躯干保守动态与动作边界验证](l3-torso-conservative-dynamic-boundary.md)。
