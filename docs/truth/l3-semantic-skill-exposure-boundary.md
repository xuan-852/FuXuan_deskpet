# L3 认证技能的 AI 暴露边界

> **验证日期**：2026-09-17  
> **状态**：已验证；未新增任何动作或原始参数入口。

`CertifiedMotionLibrary.Entry` 新增 `LlmExposed`。认证准入与 AI 工具暴露现为两道独立白名单：未赋值的条目默认隐藏；`ChatManager` 与 `RequestBodySkillTool` 仅枚举公开条目，工具执行还会再次拒绝未获授权的技能 ID。

现有六个已公开的外部认证动作被显式标记为 `true`，保持此前可见行为。未来内部躯干候选可先获得运行时或本地执行验证而不进入模型提示；在单独授权前保持不可由 AI 请求。

验证：`build.ps1 -Quick` 通过；EditMode 共 221 项，220 通过、0 失败、1 项既有忽略。新增测试断言默认条目不可暴露，未知 ID 被拒绝，既有公开条目保持显式可见。
