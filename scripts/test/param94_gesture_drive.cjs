'use strict';
const fs=require('fs'),os=require('os'),path=require('path'),{spawn}=require('child_process');
const exe=process.argv[2]; const root=path.join(os.tmpdir(),'fuxuan_param94_gesture_20260916'), inbox=path.join(root,'inbox.txt'), sleep=ms=>new Promise(r=>setTimeout(r,ms));
const log=()=>{try{return fs.readFileSync(path.join(root,'logs','player_log.txt'),'utf8')}catch{return ''}};
async function send(x,ms=350){fs.writeFileSync(inbox,x);await sleep(ms);fs.writeFileSync(inbox,'');await sleep(60)}
async function wait(mark){for(let end=Date.now()+90000;Date.now()<end;await sleep(200))if(log().includes(mark))return;throw Error('timeout '+mark)}
async function count(n){let d=path.join(root,'test_screenshots');for(let end=Date.now()+3000;Date.now()<end;await sleep(80))if(fs.existsSync(d)&&fs.readdirSync(d).filter(x=>x.endsWith('.png')).length>=n)return;throw Error('missing frame '+n)}
(async()=>{if(!exe||!fs.existsSync(exe))throw Error('missing exe');fs.rmSync(root,{recursive:true,force:true});fs.mkdirSync(root,{recursive:true});fs.writeFileSync(path.join(root,'.test_mode'),'');fs.writeFileSync(inbox,'');let p=spawn(exe,[],{env:{...process.env,FU_XUAN_DATA:root},stdio:'ignore'});try{await wait('[DesktopPet] 落地');await send('@@sim:idle-actions:off');await sleep(2500);await send('@@sim:gesture:param94');for(let i=0;i<12;i++){await send('@@sim:model-measurement-snapshot',180);await count(i+1)}await sleep(600);await send('@@sim:model-measurement-snapshot');await count(13);await send('@@test:quit',1000);console.log(root)}finally{if(!p.killed)p.kill()}})().catch(e=>{console.error(e.message);process.exitCode=1});
